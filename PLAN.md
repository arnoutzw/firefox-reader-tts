# Delivery plan

1. Verify the `openai-edge-tts` API, input limit, response format, and exact Ava Multilingual voice identifier. **Done**
2. Define Firefox permission, extraction, privacy, cancellation, and failure boundaries. **Done**
3. Build the Manifest V3 toolbar flow and Mozilla Readability extraction. **Done**
4. Build the structured reader UI and accessible playback controls. **Done**
5. Integrate cancellable background requests to `/v1/audio/speech` with Ava Multilingual. **Done**
6. Add a loopback-only Docker Compose profile, settings, tests, and documentation. **Done**
7. Run Luna/Terra implementation work, Sol code review, resolve blocking findings, lint, test, and package. **Done**
8. Add a chunk-aware playback marker with word-level highlighting, viewport following, tests, and packaging. **Done**
9. Add and verify the `Ctrl+Shift+U` current-article autoplay command. **Done**
10. Add Reader TTS multi-touch support for Magic Trackpad horizontal seek, pinch text resizing, and native vertical scrolling. **Done**
11. Add an explicit archive.ph newest-snapshot redirect, hidden-tab extraction, provenance, cleanup, permissions, and tests. **Done**
12. Fall back to creating a new archive.ph snapshot when lookup reports none, with bounded progress waiting and challenge-safe cleanup. **Done**
13. Foreground interactive archive.ph challenges, resume extraction after completion, restore Reader TTS focus, and preserve timed-out tabs. **Done**
14. Preserve Readability paragraph and semantic block boundaries through extraction, rendering, TTS chunking, and legacy fallback. **Done**
15. Add inline figures, responsive image selection, consent-gated loading, captions, retry handling, and tests. **Done**
